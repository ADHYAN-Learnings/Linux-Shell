#To display count of repeated values with an ignore case -i
#Syntax uniq -ic filename
#and to remove the extra space of above command we can use cut command with the help of cut command. For example 
uniq -ic filename | cut -c 7-
